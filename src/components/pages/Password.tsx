import { IonItem, IonTitle } from '@ionic/react';
import React, { FunctionComponent } from 'react';
export const Password: FunctionComponent<{keyId: string;}> = props => (
    <IonItem>
        <IonTitle>Title</IonTitle>
    </IonItem>
);